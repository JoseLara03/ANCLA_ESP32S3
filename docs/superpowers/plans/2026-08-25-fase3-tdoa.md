# Fase 3 — TDoA: blink, backhaul de timestamps y solve en el gateway

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Que 100 tags reporten posición a 5 Hz simultáneamente, emitiendo un
blink que la infraestructura estampa y el gateway resuelve, en lugar de que cada
tag haga TWR contra cuatro anclas y resuelva por su cuenta.

**Architecture:** El tag emite un frame `0xF0` BLINK en su slot y no espera nada.
Cada ancla que lo oye lee `dwt_readrxtimestamp()`, lo convierte al tiempo del
master con `sync_model_to_master()` y publica `(tag, seq, anchor, t_dtu)` por
MQTT. El gateway se suscribe a ese tópico, agrupa las observaciones del mismo
blink, y resuelve la posición por multilateración hiperbólica sobre las
*diferencias* de tiempo. Río abajo del solver nada cambia: sigue produciendo un
`struct pos_result` y publicándose por `pos_sink_publish()`.

**Tech Stack:** C99 puro para toda la matemática (host-testable con gcc, sin
Zephyr y sin CMSIS); Zephyr 4.4.x + MQTT para el backhaul; DW3220 vía el módulo
vendorizado.

**Spec:** `docs/superpowers/specs/2026-08-25-rtls-scale-tdoa-design.md` §4.3 y
Fase 3 de §5.

---

## Condicionalidad — leer antes de empezar

**Este plan está escrito ANTES del gate de Fase 2** para tener ventaja, a
petición explícita. El gate es el de `docs/anchor-sync-measurement.md`: jitter de
timestamp ancla-a-ancla bajo ~0.5 ns.

Qué depende de qué:

| Tareas | Dependen del gate | Por qué |
|---|---|---|
| **1–4** | **No** | Matemática y formato de aire puros. Valen aunque el gate falle: el solver TDoA y el colector son correctos independientemente de si la sincronía alcanza. |
| 5–6 | **Sí** | El backhaul y el solve en vivo no tienen sentido sin sincronía sub-ns. |
| 7–8 | **Sí** | Retirar el solver del tag es irreversible en la práctica; no hacerlo hasta que el gateway resuelva de verdad. |

**Empezar por 1–4.** Si el gate falla o sale marginal, ver "Contingencias" al
final: varias tareas sobreviven con cambios acotados.

---

## Global Constraints

Copiados literalmente del spec y de `CLAUDE.md`; aplican a **todas** las tareas.

- **PHY congelado:** canal 5, PLEN_1024, PAC32, código 9, 850 kbps, SFD_IEEE_4Z,
  STS y PDoA off. Ninguna tarea lo toca. Cambiarlo invalida la calibración de
  retardo de antena de toda la flota.
- **`src/uwb_frame_802_15_4z.{c,h}` y `src/cal_math.{c,h}` son byte-idénticos
  con `tag_testting`.** El BLINK **no** va ahí — módulo propio, como
  `apos_frame.c` y `ccp_frame.c`. Verificar con `diff` antes de cerrar cualquier
  tarea que los toque; debe salir vacío.
- **Contabilidad de longitudes: los bytes EXCLUYEN el FCS**, igual que
  `UWB_FRAME_LEN_*`. `dwt_getframelength()` y `cb_data->datalength` lo
  **incluyen**; los validadores usan `len >=`, no `len ==`.
- **El loop del gateway corre en `K_PRIO_COOP(0)`.** Nada en esa ruta puede
  bloquear ni esperar sin cota. Ninguna escritura a flash sin gate de
  presupuesto.
- **Toda comparación entre timestamps debe ser diferencia con signo.** El
  contador hi32 da la vuelta cada ~17.2 s y el de 40 bits también.
- **Sin float en rutas de tiempo/scheduling.** El solver sí usa float (como el
  del tag); la conversión de reloj es entera.
- Host tests: `gcc -Wall -Wextra -Isrc`, un `main()` por suite, `CHECK()`,
  salida distinta de cero al fallar. `-lm` solo donde se use `<math.h>`.
- Mensajes de commit terminan con
  `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`.

### Constantes que este plan introduce

| Constante | Valor | De dónde sale |
|---|---|---|
| `BLINK_FRAME_TYPE` | `0xF0` | Primer código libre. `0xE0..0xEF` está lleno — ver la tabla en `src/ccp_frame.h`. Verificado libre en **ambos** repos. |
| `TDOA_M_PER_DTU` | `4.69175e-3f` | `c / (499.2e6 × 128)` = `299792458 × 1.565004e-11` m. Un DTU son **4.69 mm** de diferencia de camino. |
| `TDOA_MIN_ANCHORS` | `3` | 2D necesita 2 diferencias independientes, y n anclas dan n−1. |

---

## Estructura de archivos

**Nuevos, C puro y host-testeable (el adelanto real):**

| Archivo | Responsabilidad |
|---|---|
| `src/blink_frame.{c,h}` | Formato de aire del BLINK `0xF0`. Solo build/parse/validate. |
| `src/tdoa_solve.{c,h}` | Gauss-Newton sobre el modelo de **diferencia** de rangos. |
| `src/tdoa_collect.{c,h}` | Agrupa observaciones del mismo blink; ventana, timeout, mínimo de anclas. |
| `src/pos_solver.{c,h}` | **Portado verbatim** del tag. Trae `struct pos_meas` y `struct pos_result`. |
| `src/pos_residual.{c,h}` | **Portado verbatim** del tag. |

**Nuevos, requieren hardware:**

| Archivo | Responsabilidad |
|---|---|
| `src/blink_rx.{c,h}` | Lado ancla: estampa el BLINK, convierte con `sync_model`, encola para el uplink. |
| `src/tdoa_gw.{c,h}` | Lado gateway: consume del colector, resuelve, entrega a `pos_sink`. |

**Modificados:**

| Archivo | Cambio |
|---|---|
| `src/net_uplink.{c,h}` | Añadir **subscribe** (hoy es solo-publish) y arrancar también en modo SLAVE. |
| `src/uwb_slave.c` | Despachar `0xF0` a `blink_rx`. |
| `src/uwb_gateway.c` | Alimentar `tdoa_gw` desde el colector. |
| `CMakeLists.txt`, `CLAUDE.md` | Registrar módulos y suites. |

**Del lado del tag (`tag_testting`), al final:**

| Archivo | Cambio |
|---|---|
| `src/blink_frame.{c,h}` | Copia byte-idéntica desde ANCLA. |
| `src/uwb_net_runner.c` | Emitir BLINK en vez de barrer y resolver. |
| `src/pos_dbg.{c,h}` | Borrar, según el checklist de su spec. |

---

## Task 1: Formato de aire del BLINK

**Files:**
- Create: `src/blink_frame.h`, `src/blink_frame.c`
- Test: `tests/blink_frame/test_blink_frame.c`
- Modify: `CMakeLists.txt` (añadir `src/blink_frame.c` en orden alfabético)

**Interfaces:**
- Consumes: nada.
- Produces: `BLINK_FRAME_TYPE` (`0xF0`), `BLINK_FRAME_LEN` (`14`),
  `struct blink_frame { uint16_t src_addr; uint8_t seq; uint8_t batt_soc; uint8_t flags; }`,
  `int blink_frame_build(uint8_t *buf, size_t buf_len, uint16_t src_addr, uint8_t frame_seq, const struct blink_frame *f)`,
  `bool blink_frame_is_blink(const uint8_t *buf, size_t len)`,
  `int blink_frame_parse(const uint8_t *buf, size_t len, struct blink_frame *out)`.

**Layout** (offsets absolutos; longitud excluye FCS):

```
0..9    header 802.15.4: 0x41 0x88, seq, PANID 0xCA 0xDE, dest 0xFFFF, src, type 0xF0
10      blink_seq   monotónico, da la vuelta en 256; la clave de agrupación
11      batt_soc    0..100, o UWB_FRAME_POS_SOC_UNKNOWN (0xFF)
12      flags       bit0 = ALERT pendiente; bits 1..7 reservados, deben ser 0
13      reserved    debe ser 0; existe para que la longitud sea par
```

- [ ] **Step 1: Escribir el test que falla**

Crear `tests/blink_frame/test_blink_frame.c`:

```c
#include <stdio.h>
#include <stdlib.h>
#include <errno.h>
#include "blink_frame.h"

static int failures;
#define CHECK(c) do { if (!(c)) { \
    printf("FAIL %s:%d: %s\n", __FILE__, __LINE__, #c); failures++; } } while (0)

/* El código de función es un valor de AIRE asignado entre dos repos. Fijado
 * aquí para que cambiarlo sea una edición deliberada a un test -- que 0xEB se
 * repartiera dos veces fue exactamente por no tener esto. */
static void test_wire_constants(void)
{
    CHECK(BLINK_FRAME_TYPE == 0xF0);
    CHECK(BLINK_FRAME_LEN == 14);
}

static void test_roundtrip(void)
{
    uint8_t buf[BLINK_FRAME_LEN];
    struct blink_frame in = { .seq = 0x5A, .batt_soc = 77, .flags = 0x01 };
    struct blink_frame out;

    CHECK(blink_frame_build(buf, sizeof(buf), 0x0101, 0x22, &in) ==
          (int)BLINK_FRAME_LEN);
    CHECK(buf[0] == 0x41 && buf[1] == 0x88);
    CHECK(buf[2] == 0x22);
    CHECK(buf[3] == 0xCA && buf[4] == 0xDE);
    CHECK(buf[5] == 0xFF && buf[6] == 0xFF);
    CHECK(buf[7] == 0x01 && buf[8] == 0x01);
    CHECK(buf[9] == BLINK_FRAME_TYPE);
    CHECK(buf[13] == 0);

    CHECK(blink_frame_is_blink(buf, sizeof(buf)));
    CHECK(blink_frame_parse(buf, sizeof(buf), &out) == 0);
    CHECK(out.src_addr == 0x0101);
    CHECK(out.seq == 0x5A);
    CHECK(out.batt_soc == 77);
    CHECK(out.flags == 0x01);
}

/* Igual que ccp_frame: dwt_getframelength() INCLUYE el FCS, así que un test
 * `len ==` tiraría todo frame real de un llamador que no lo restó. */
static void test_length_tolerates_the_fcs(void)
{
    uint8_t buf[BLINK_FRAME_LEN + 2];
    struct blink_frame in = { .seq = 1, .batt_soc = 50, .flags = 0 };

    CHECK(blink_frame_build(buf, sizeof(buf), 1, 0, &in) > 0);
    CHECK(blink_frame_is_blink(buf, BLINK_FRAME_LEN));
    CHECK(blink_frame_is_blink(buf, BLINK_FRAME_LEN + 2));
    CHECK(!blink_frame_is_blink(buf, BLINK_FRAME_LEN - 1));
}

/* Los bits reservados deben ser cero. Aceptarlos hoy hace imposible usarlos
 * mañana sin romper compatibilidad. */
static void test_reserved_bits_rejected(void)
{
    uint8_t buf[BLINK_FRAME_LEN];
    struct blink_frame in = { .seq = 1, .batt_soc = 50, .flags = 0 };
    struct blink_frame out;

    CHECK(blink_frame_build(buf, sizeof(buf), 1, 0, &in) > 0);
    buf[12] = 0x02;
    CHECK(blink_frame_parse(buf, sizeof(buf), &out) == -EPROTO);
    buf[12] = 0x00;
    buf[13] = 0x01;
    CHECK(blink_frame_parse(buf, sizeof(buf), &out) == -EPROTO);
}

static void test_rejects_other_codes(void)
{
    uint8_t buf[BLINK_FRAME_LEN];
    struct blink_frame in = { .seq = 1, .batt_soc = 0, .flags = 0 };
    struct blink_frame out;
    const uint8_t others[] = { 0xE0, 0xE5, 0xEA, 0xEB, 0xEE, 0xEF };

    CHECK(blink_frame_build(buf, sizeof(buf), 1, 0, &in) > 0);
    for (unsigned int i = 0; i < sizeof(others); i++) {
        buf[9] = others[i];
        CHECK(!blink_frame_is_blink(buf, sizeof(buf)));
        CHECK(blink_frame_parse(buf, sizeof(buf), &out) == -EINVAL);
    }
}

static void test_arg_checks(void)
{
    uint8_t buf[BLINK_FRAME_LEN];
    struct blink_frame in = { .seq = 1, .batt_soc = 0, .flags = 0 };

    CHECK(blink_frame_build(NULL, sizeof(buf), 1, 0, &in) == -EINVAL);
    CHECK(blink_frame_build(buf, sizeof(buf), 1, 0, NULL) == -EINVAL);
    CHECK(blink_frame_build(buf, BLINK_FRAME_LEN - 1, 1, 0, &in) == -EMSGSIZE);
    CHECK(!blink_frame_is_blink(NULL, BLINK_FRAME_LEN));
}

/* El airtime es el presupuesto entero de la Fase 3: 100 tags a 5 Hz son 500
 * blinks/s. Registrado para que el costo esté a la vista. */
static void test_airtime(void)
{
    const unsigned int us = 1042 + 8 + 22 + (BLINK_FRAME_LEN + 2) * 941 / 100;

    CHECK(us > 1200 && us < 1250);
    printf("  BLINK airtime: ~%u us; 500/s = %.1f %% de ocupacion\n",
           us, (double)us * 500.0 / 10000.0);
}

int main(void)
{
    test_wire_constants();
    test_roundtrip();
    test_length_tolerates_the_fcs();
    test_reserved_bits_rejected();
    test_rejects_other_codes();
    test_arg_checks();
    test_airtime();
    if (failures) { printf("\n%d CHECK(s) FAILED\n", failures); return EXIT_FAILURE; }
    printf("blink_frame: ALL TESTS PASSED\n");
    return EXIT_SUCCESS;
}
```

- [ ] **Step 2: Correr el test y verificar que NO compila**

```bash
gcc -Wall -Wextra -Isrc -o tests/blink_frame/test_blink_frame.exe \
    tests/blink_frame/test_blink_frame.c src/blink_frame.c
```
Esperado: FALLA con `blink_frame.h: No such file or directory`.

- [ ] **Step 3: Escribir el header**

Crear `src/blink_frame.h`. Debe llevar, en el comentario de cabecera: que
`0xF0` es el primer código fuera del rango `0xEx` (lleno, ver `ccp_frame.h`);
que este archivo **no** va en `uwb_frame_802_15_4z.c` por la regla de
byte-identidad; y que el tag lleva una **copia byte-idéntica**, así que la
fuente de verdad es este repo.

```c
#ifndef BLINK_FRAME_H
#define BLINK_FRAME_H

#include <stdint.h>
#include <stddef.h>
#include <stdbool.h>

#define BLINK_FRAME_TYPE  0xF0u
#define BLINK_FRAME_LEN   14u

#define BLINK_OFF_SEQ     10u
#define BLINK_OFF_SOC     11u
#define BLINK_OFF_FLAGS   12u
#define BLINK_OFF_RSVD    13u

#define BLINK_FLAG_ALERT          0x01u
#define BLINK_FLAG_RESERVED_MASK  0xFEu

struct blink_frame {
    uint16_t src_addr;
    uint8_t  seq;
    uint8_t  batt_soc;
    uint8_t  flags;
};

int  blink_frame_build(uint8_t *buf, size_t buf_len, uint16_t src_addr,
                       uint8_t frame_seq, const struct blink_frame *f);
bool blink_frame_is_blink(const uint8_t *buf, size_t len);
int  blink_frame_parse(const uint8_t *buf, size_t len, struct blink_frame *out);

#endif /* BLINK_FRAME_H */
```

- [ ] **Step 4: Escribir la implementación mínima**

Crear `src/blink_frame.c`, copiando el estilo de `src/ccp_frame.c` (helpers
little-endian propios; no exportar internos del codec compartido).

```c
#include "blink_frame.h"
#include <errno.h>

#define OFF_SEQ_HDR 2u
#define OFF_DEST    5u
#define OFF_SRC     7u
#define OFF_TYPE    9u
#define ADDR_BCAST  0xFFFFu

static void put_u16(uint8_t *p, uint16_t v)
{
    p[0] = (uint8_t)v;
    p[1] = (uint8_t)(v >> 8);
}

static uint16_t get_u16(const uint8_t *p)
{
    return (uint16_t)(p[0] | ((uint16_t)p[1] << 8));
}

int blink_frame_build(uint8_t *buf, size_t buf_len, uint16_t src_addr,
                      uint8_t frame_seq, const struct blink_frame *f)
{
    if (!buf || !f) return -EINVAL;
    if (buf_len < BLINK_FRAME_LEN) return -EMSGSIZE;

    buf[0] = 0x41;
    buf[1] = 0x88;
    buf[OFF_SEQ_HDR] = frame_seq;
    buf[3] = 0xCA;
    buf[4] = 0xDE;
    put_u16(&buf[OFF_DEST], ADDR_BCAST);
    put_u16(&buf[OFF_SRC], src_addr);
    buf[OFF_TYPE] = BLINK_FRAME_TYPE;

    buf[BLINK_OFF_SEQ]   = f->seq;
    buf[BLINK_OFF_SOC]   = f->batt_soc;
    buf[BLINK_OFF_FLAGS] = (uint8_t)(f->flags & ~BLINK_FLAG_RESERVED_MASK);
    buf[BLINK_OFF_RSVD]  = 0;
    return (int)BLINK_FRAME_LEN;
}

bool blink_frame_is_blink(const uint8_t *buf, size_t len)
{
    return buf && len >= BLINK_FRAME_LEN && buf[0] == 0x41 && buf[1] == 0x88 &&
           buf[3] == 0xCA && buf[4] == 0xDE && buf[OFF_TYPE] == BLINK_FRAME_TYPE;
}

int blink_frame_parse(const uint8_t *buf, size_t len, struct blink_frame *out)
{
    if (!out) return -EINVAL;
    if (!blink_frame_is_blink(buf, len)) return -EINVAL;
    if (buf[BLINK_OFF_FLAGS] & BLINK_FLAG_RESERVED_MASK) return -EPROTO;
    if (buf[BLINK_OFF_RSVD] != 0) return -EPROTO;

    out->src_addr = get_u16(&buf[OFF_SRC]);
    out->seq      = buf[BLINK_OFF_SEQ];
    out->batt_soc = buf[BLINK_OFF_SOC];
    out->flags    = buf[BLINK_OFF_FLAGS];
    return 0;
}
```

- [ ] **Step 5: Correr el test y verificar que pasa**

```bash
gcc -Wall -Wextra -Isrc -o tests/blink_frame/test_blink_frame.exe \
    tests/blink_frame/test_blink_frame.c src/blink_frame.c && \
    ./tests/blink_frame/test_blink_frame.exe
```
Esperado: `blink_frame: ALL TESTS PASSED`, salida 0, cero warnings.

- [ ] **Step 6: Registrar y documentar**

Añadir `src/blink_frame.c` a `target_sources(app PRIVATE ...)` en
`CMakeLists.txt`, en orden alfabético (va justo después de `src/beacon_guard.c`
y antes de `src/ccp_frame.c`). Crear `tests/blink_frame/.gitignore` con `*.exe`.
Añadir el módulo a la sección **Layout** de `CLAUDE.md` y su línea de compilación
a **Host tests**. Actualizar la tabla de asignación de códigos en
`src/ccp_frame.h` para que `0xF0` aparezca como tomado.

- [ ] **Step 7: Verificar que el build de Zephyr sigue limpio**

```bash
west build -b ancla_esp32s3/esp32s3/procpu -d build_prod
```
Esperado: 0 errores, 0 warnings. **Correrlo siempre**: gcc de host resuelve
includes que el toolchain de Zephyr no — `mac_budget.c` falló exactamente así
con `NULL` sin `<stddef.h>`.

- [ ] **Step 8: Commit**

```bash
git add src/blink_frame.c src/blink_frame.h tests/blink_frame \
        src/ccp_frame.h CMakeLists.txt CLAUDE.md
git commit -m "feat(tdoa): BLINK frame, function code 0xF0

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

## Task 2: Portar el solver de rangos del tag

Mecánico y primero, porque la Tarea 3 necesita `struct pos_result` y
`pos_residual_rms()`. Los tres archivos ya son C puro sin CMSIS ni Zephyr, así
que el porte es una copia.

**Files:**
- Create: `src/pos_solver.h`, `src/pos_solver.c`, `src/pos_residual.h`,
  `src/pos_residual.c` (copias de `tag_testting/src/`)
- Test: `tests/pos_solver/test_pos_solver.c`,
  `tests/pos_residual/test_pos_residual.c` (copias de `tag_testting/tests/`)
- Modify: `CMakeLists.txt`, `CLAUDE.md`

**Interfaces:**
- Consumes: nada.
- Produces: `struct pos_meas { float x, y, dz, range_m; }`,
  `struct pos_result { float x, y, residual_m; bool valid; uint8_t n_used, dropped_idx; }`,
  `POS_MAX_ANCHORS` (4), `POS_NO_DROP` (0xFF),
  `bool pos_solve(const struct pos_meas *m, size_t n, const float *seed_xy, struct pos_result *out)`,
  `float pos_residual_rms(const struct pos_meas *m, size_t n, float x, float y)`.

- [ ] **Step 1: Copiar los cuatro archivos fuente y los dos de test**

```bash
T=../../../tag_testting          # ajustar si la ruta relativa difiere
for f in pos_solver.c pos_solver.h pos_residual.c pos_residual.h; do
    cp "$T/src/$f" src/$f
done
mkdir -p tests/pos_solver tests/pos_residual
cp "$T/tests/pos_solver/test_pos_solver.c"   tests/pos_solver/
cp "$T/tests/pos_residual/test_pos_residual.c" tests/pos_residual/
```

- [ ] **Step 2: Verificar identidad byte a byte**

```bash
for f in pos_solver.c pos_solver.h pos_residual.c pos_residual.h; do
    diff "src/$f" "$T/src/$f" || echo "DIVERGENTE: $f"
done
```
Esperado: sin salida. **Estos archivos se vuelven compartidos**, así que aplica
la misma regla de §8.0 del spec: el tag los sigue poseyendo mientras siga
resolviendo; tras la Tarea 8 la propiedad pasa a este repo. Anotarlo en ambos
`CLAUDE.md`.

- [ ] **Step 3: Correr las suites portadas**

```bash
gcc -Wall -Wextra -Isrc -o tests/pos_residual/test_pos_residual.exe \
    tests/pos_residual/test_pos_residual.c src/pos_residual.c -lm && \
    ./tests/pos_residual/test_pos_residual.exe
gcc -Wall -Wextra -Isrc -o tests/pos_solver/test_pos_solver.exe \
    tests/pos_solver/test_pos_solver.c src/pos_solver.c src/pos_residual.c -lm && \
    ./tests/pos_solver/test_pos_solver.exe
```
Esperado: ambas pasan sin modificación. **Si alguna falla, el porte está mal**,
no los tests — son C puro y no dependen de nada del tag.

- [ ] **Step 4: Registrar**

Añadir `src/pos_residual.c` y `src/pos_solver.c` a `CMakeLists.txt` en orden
alfabético (entre `src/pos_json.c` y `src/pos_sink.c`). Crear los dos
`.gitignore` con `*.exe`. Añadir ambos módulos y sus líneas de compilación a
`CLAUDE.md`, indicando que necesitan `-lm`.

- [ ] **Step 5: Verificar el build de Zephyr**

```bash
west build -b ancla_esp32s3/esp32s3/procpu -d build_prod
```
Esperado: 0 errores, 0 warnings. **Vigilar el uso de flash**: se añade `sqrtf`
al gateway, que ya enlazaba libm por `apos_geom.c`, así que el delta debe ser
pequeño. Un salto grande significa que se coló algo más.

- [ ] **Step 6: Commit**

```bash
git add src/pos_solver.* src/pos_residual.* tests/pos_solver tests/pos_residual \
        CMakeLists.txt CLAUDE.md
git commit -m "feat(tdoa): port the tag's range solver to the gateway verbatim

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

## Task 3: El solver TDoA

El núcleo matemático de la Fase 3, y es un **cambio de modelo de medición sobre
la misma maquinaria**, no un rewrite.

**Files:**
- Create: `src/tdoa_solve.h`, `src/tdoa_solve.c`
- Test: `tests/tdoa_solve/test_tdoa_solve.c`
- Modify: `CMakeLists.txt`, `CLAUDE.md`

**Interfaces:**
- Consumes: `struct pos_result` y `POS_MAX_ANCHORS` de `pos_solver.h` (Tarea 2).
- Produces: `TDOA_M_PER_DTU` (`4.69175e-3f`), `TDOA_MIN_ANCHORS` (`3`),
  `struct tdoa_meas { float x, y, dz; int64_t t_dtu; }`,
  `bool tdoa_solve(const struct tdoa_meas *m, size_t n, const float *seed_xy, struct pos_result *out)`.

**La matemática, para que el implementador no la derive mal.** Con el ancla 0
como referencia y `r_i(x,y) = sqrt((x−x_i)² + (y−y_i)² + dz_i²)`, cada ancla
`i ≥ 1` aporta una ecuación:

```
f_i(x, y) = ( r_i(x,y) − r_0(x,y) )  −  d_i
d_i       = (t_i − t_0) × TDOA_M_PER_DTU
∂f_i/∂x   = (x − x_i)/r_i − (x − x_0)/r_0
∂f_i/∂y   = (y − y_i)/r_i − (y − y_0)/r_0
```

`n` anclas dan `n−1` ecuaciones y hay 2 incógnitas: con 3 anclas queda
exactamente determinado, con 4 sobredeterminado. Las ecuaciones normales son
2×2 y se invierten en forma cerrada, **igual que en `pos_solver.c`** — copiar de
ahí el patrón de iteración, el test de determinante y los criterios de parada en
vez de inventarlos.

- [ ] **Step 1: Escribir el test que falla**

Crear `tests/tdoa_solve/test_tdoa_solve.c`. El generador sintético es la pieza
clave: coloca anclas, elige una posición verdadera, calcula los tiempos de
llegada exactos, y comprueba que el solver recupera la posición.

```c
#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include "tdoa_solve.h"

static int failures;
#define CHECK(c) do { if (!(c)) { \
    printf("FAIL %s:%d: %s\n", __FILE__, __LINE__, #c); failures++; } } while (0)

/* Anclas en las esquinas de un cuadrado de 10 m, tag 1.5 m debajo de ellas. */
static const float AX[4] = { 0.0f, 10.0f, 10.0f,  0.0f };
static const float AY[4] = { 0.0f,  0.0f, 10.0f, 10.0f };
#define DZ 1.5f

/* Construye observaciones EXACTAS para una posición verdadera. Los tiempos son
 * enteros en DTU, como llegan del hardware, con un offset común arbitrario que
 * el solver debe cancelar -- ese es todo el punto de TDoA. */
static void make(struct tdoa_meas *m, size_t n, float tx, float ty,
                 int64_t common_offset)
{
    for (size_t i = 0; i < n; i++) {
        float dx = tx - AX[i], dy = ty - AY[i];
        float r = sqrtf(dx * dx + dy * dy + DZ * DZ);

        m[i].x = AX[i];
        m[i].y = AY[i];
        m[i].dz = DZ;
        m[i].t_dtu = common_offset + (int64_t)llroundf(r / TDOA_M_PER_DTU);
    }
}

static void test_recovers_a_known_position(void)
{
    const float pts[][2] = { {5.0f, 5.0f}, {2.0f, 3.0f}, {7.5f, 8.0f},
                             {1.0f, 1.0f}, {9.0f, 5.0f} };

    for (unsigned int k = 0; k < 5; k++) {
        struct tdoa_meas m[4];
        struct pos_result r;

        make(m, 4, pts[k][0], pts[k][1], 0x1234567890LL);
        CHECK(tdoa_solve(m, 4, NULL, &r));
        CHECK(r.valid);
        CHECK(fabsf(r.x - pts[k][0]) < 0.05f);
        CHECK(fabsf(r.y - pts[k][1]) < 0.05f);
        printf("  (%.1f,%.1f) -> (%.3f,%.3f) res %.4f\n",
               (double)pts[k][0], (double)pts[k][1],
               (double)r.x, (double)r.y, (double)r.residual_m);
    }
}

/* El offset común es lo que TDoA cancela por construcción: el tag no tiene
 * reloj compartido con nadie. Cambiarlo no debe mover el resultado ni un mm. */
static void test_common_offset_cancels(void)
{
    struct tdoa_meas a[4], b[4];
    struct pos_result ra, rb;

    make(a, 4, 4.0f, 6.0f, 0);
    make(b, 4, 4.0f, 6.0f, 0x7FFFFFFFFLL);
    CHECK(tdoa_solve(a, 4, NULL, &ra));
    CHECK(tdoa_solve(b, 4, NULL, &rb));
    CHECK(ra.valid && rb.valid);
    CHECK(fabsf(ra.x - rb.x) < 1e-3f);
    CHECK(fabsf(ra.y - rb.y) < 1e-3f);
}

/* Tres anclas es el mínimo: 2 diferencias, 2 incógnitas. */
static void test_three_anchors_is_enough(void)
{
    struct tdoa_meas m[3];
    struct pos_result r;

    make(m, 3, 4.0f, 4.0f, 99);
    CHECK(tdoa_solve(m, 3, NULL, &r));
    CHECK(r.valid);
    CHECK(fabsf(r.x - 4.0f) < 0.10f);
    CHECK(fabsf(r.y - 4.0f) < 0.10f);
}

static void test_too_few_anchors_refused(void)
{
    struct tdoa_meas m[4];
    struct pos_result r;

    make(m, 4, 5.0f, 5.0f, 0);
    CHECK(!tdoa_solve(m, 2, NULL, &r));
    CHECK(!r.valid);
    CHECK(!tdoa_solve(m, 0, NULL, &r));
    CHECK(!tdoa_solve(NULL, 4, NULL, &r));
}

/* Anclas colineales dejan la matriz normal singular. Debe reportarse, no
 * devolver un punto inventado -- igual que pos_solve. */
static void test_collinear_anchors_refused(void)
{
    struct tdoa_meas m[3];
    struct pos_result r;

    for (int i = 0; i < 3; i++) {
        m[i].x = (float)i * 5.0f;
        m[i].y = 0.0f;
        m[i].dz = DZ;
        m[i].t_dtu = 1000 + i * 100;
    }
    CHECK(!tdoa_solve(m, 3, NULL, &r));
    CHECK(!r.valid);
}

/* Un DTU son 4.69 mm de diferencia de camino: ésa es la resolución que la
 * sincronía tiene que sostener, y la constante que convierte el gate de la
 * Fase 2 en error de posición. */
static void test_dtu_scale(void)
{
    CHECK(TDOA_M_PER_DTU > 0.00469f && TDOA_M_PER_DTU < 0.00470f);
    /* 1 ns de error de sincronía = 64 DTU = ~30 cm. */
    CHECK(fabsf(64.0f * TDOA_M_PER_DTU - 0.300f) < 0.01f);
}

/* Ruido de sincronía a nivel del gate: 0.5 ns = 32 DTU sobre cada timestamp. El
 * error de posición resultante es lo que decide si el producto cumple 10-30 cm,
 * y este test lo IMPRIME en vez de afirmarlo -- el número es la entrega. */
static void test_error_under_gate_level_noise(void)
{
    float worst = 0.0f;
    uint32_t rng = 12345u;

    for (unsigned int trial = 0; trial < 200u; trial++) {
        struct tdoa_meas m[4];
        struct pos_result r;

        make(m, 4, 5.0f, 5.0f, 1000000);
        for (int i = 0; i < 4; i++) {
            rng = rng * 1664525u + 1013904223u;
            m[i].t_dtu += (int64_t)((rng >> 8) % 65u) - 32;   /* +/-32 DTU */
        }
        if (!tdoa_solve(m, 4, NULL, &r) || !r.valid) continue;
        float e = sqrtf((r.x - 5.0f) * (r.x - 5.0f) +
                        (r.y - 5.0f) * (r.y - 5.0f));
        if (e > worst) worst = e;
    }
    printf("  con +/-32 DTU (0.5 ns) de ruido: peor error %.3f m\n",
           (double)worst);
    CHECK(worst < 1.0f);
}

int main(void)
{
    test_dtu_scale();
    test_recovers_a_known_position();
    test_common_offset_cancels();
    test_three_anchors_is_enough();
    test_too_few_anchors_refused();
    test_collinear_anchors_refused();
    test_error_under_gate_level_noise();
    if (failures) { printf("\n%d CHECK(s) FAILED\n", failures); return EXIT_FAILURE; }
    printf("tdoa_solve: ALL TESTS PASSED\n");
    return EXIT_SUCCESS;
}
```

- [ ] **Step 2: Correr y verificar que no compila**

```bash
gcc -Wall -Wextra -Isrc -o tests/tdoa_solve/test_tdoa_solve.exe \
    tests/tdoa_solve/test_tdoa_solve.c src/tdoa_solve.c src/pos_residual.c -lm
```
Esperado: FALLA con `tdoa_solve.h: No such file or directory`.

- [ ] **Step 3: Escribir el header**

```c
#ifndef TDOA_SOLVE_H
#define TDOA_SOLVE_H

#include <stdint.h>
#include <stddef.h>
#include <stdbool.h>
#include "pos_solver.h"     /* struct pos_result, POS_MAX_ANCHORS */

/* Metros de diferencia de camino por DTU: c / (499.2 MHz x 128).
 * 299792458 x 1.565004e-11 = 4.69175e-3. Un DTU son 4.69 mm, y 1 ns de error
 * de sincronia (64 DTU) son ~30 cm -- que es como el gate de la Fase 2 se
 * traduce a error de posicion. */
#define TDOA_M_PER_DTU  4.69175e-3f

/* 2D necesita 2 diferencias independientes, y n anclas dan n-1. */
#define TDOA_MIN_ANCHORS  3u

/* Una observacion: donde esta el ancla, y cuando oyo el blink EN EL TIEMPO
 * COMUN (ya convertido por sync_model_to_master()). `dz` sigue la misma
 * convencion que struct pos_meas: ancla z menos tag z, en metros, dentro del
 * modelo y no proyectado fuera de antemano. */
struct tdoa_meas {
    float   x;
    float   y;
    float   dz;
    int64_t t_dtu;
};

/* Multilateracion hiperbolica 2D por Gauss-Newton sobre diferencias de rango,
 * con el ancla 0 como referencia. Mismo esqueleto que pos_solve(): ecuaciones
 * normales 2x2 invertidas en forma cerrada, con el determinante probado
 * explicitamente para la geometria degenerada.
 *
 * Devuelve false (y out->valid = false) si n < TDOA_MIN_ANCHORS, si la
 * geometria es degenerada, o si la iteracion no converge. */
bool tdoa_solve(const struct tdoa_meas *m, size_t n, const float *seed_xy,
                struct pos_result *out);

#endif /* TDOA_SOLVE_H */
```

- [ ] **Step 4: Escribir la implementación**

Crear `src/tdoa_solve.c`. **Abrir `src/pos_solver.c` en paralelo**: los valores
de `MAX_ITER`, del epsilon de convergencia y del umbral de determinante deben
copiarse de ahí, no reinventarse — son constantes ya probadas en hardware y
divergir introduce diferencias sutiles de comportamiento entre los dos solvers.

```c
#include "tdoa_solve.h"
#include <math.h>

/* Copiar de src/pos_solver.c, no elegir de nuevo. */
#define MAX_ITER     12
#define CONV_EPS_M   1e-4f
#define DET_EPS      1e-9f

/* Distancia 3D del punto (x,y) al ancla i, con dz dentro del modelo. */
static float slant(const struct tdoa_meas *a, float x, float y)
{
    float dx = x - a->x, dy = y - a->y;

    return sqrtf(dx * dx + dy * dy + a->dz * a->dz);
}

/* RMS de f_i sobre las n-1 ecuaciones de diferencia, en metros.
 *
 * Deliberadamente NO usa pos_residual_rms(): aquel mide desacuerdo de RANGO
 * contra rangos medidos, y aqui no hay rangos medidos -- solo diferencias. Un
 * fix TDoA perfecto tiene residual de diferencia cero y un residual de rango
 * que no significa nada, porque el rango absoluto nunca se observo. */
static float diff_residual(const struct tdoa_meas *m, size_t n, float x, float y)
{
    float r0 = slant(&m[0], x, y);
    float acc = 0.0f;

    for (size_t i = 1; i < n; i++) {
        float d_meas = (float)(m[i].t_dtu - m[0].t_dtu) * TDOA_M_PER_DTU;
        float f = (slant(&m[i], x, y) - r0) - d_meas;

        acc += f * f;
    }
    return sqrtf(acc / (float)(n - 1));
}

bool tdoa_solve(const struct tdoa_meas *m, size_t n, const float *seed_xy,
                struct pos_result *out)
{
    if (!out) return false;

    out->valid = false;
    out->x = 0.0f;
    out->y = 0.0f;
    out->residual_m = 0.0f;
    out->n_used = 0;
    out->dropped_idx = POS_NO_DROP;

    if (!m || n < TDOA_MIN_ANCHORS || n > POS_MAX_ANCHORS) return false;

    float x, y;

    if (seed_xy) {
        x = seed_xy[0];
        y = seed_xy[1];
    } else {
        /* Centroide de las anclas: interior al casco convexo, que es donde la
         * geometria TDoA esta bien condicionada. Fuera de el las hiperbolas se
         * aplanan y la iteracion se va. */
        x = 0.0f;
        y = 0.0f;
        for (size_t i = 0; i < n; i++) { x += m[i].x; y += m[i].y; }
        x /= (float)n;
        y /= (float)n;
    }

    for (int it = 0; it < MAX_ITER; it++) {
        float jtj00 = 0.0f, jtj01 = 0.0f, jtj11 = 0.0f;
        float jtf0 = 0.0f, jtf1 = 0.0f;
        float r0 = slant(&m[0], x, y);

        /* dz distinto de cero mantiene r0 lejos de cero, pero un despliegue con
         * dz = 0 y el tag justo sobre el ancla lo alcanza. */
        if (r0 < 1e-6f) return false;

        for (size_t i = 1; i < n; i++) {
            float ri = slant(&m[i], x, y);

            if (ri < 1e-6f) return false;

            float d_meas = (float)(m[i].t_dtu - m[0].t_dtu) * TDOA_M_PER_DTU;
            float f  = (ri - r0) - d_meas;
            float gx = (x - m[i].x) / ri - (x - m[0].x) / r0;
            float gy = (y - m[i].y) / ri - (y - m[0].y) / r0;

            jtj00 += gx * gx;
            jtj01 += gx * gy;
            jtj11 += gy * gy;
            jtf0  += gx * f;
            jtf1  += gy * f;
        }

        /* Ecuaciones normales 2x2, invertidas en forma cerrada. Mismo patron
         * que pos_solver.c, y por la misma razon: es mas barato que una
         * inversion generica y deja probar el determinante explicitamente,
         * que es como se detecta la geometria degenerada (anclas colineales)
         * en vez de devolver un punto inventado. */
        float det = jtj00 * jtj11 - jtj01 * jtj01;

        if (fabsf(det) < DET_EPS) return false;

        float dx = -( jtj11 * jtf0 - jtj01 * jtf1) / det;
        float dy = -(-jtj01 * jtf0 + jtj00 * jtf1) / det;

        x += dx;
        y += dy;

        if (fabsf(dx) < CONV_EPS_M && fabsf(dy) < CONV_EPS_M) {
            out->x = x;
            out->y = y;
            out->residual_m = diff_residual(m, n, x, y);
            out->n_used = (uint8_t)n;
            /* El rechazo de outliers NO se porta aqui, deliberadamente. Con 3
             * anclas no hay redundancia que lo permita, y con 4 el criterio de
             * subconjuntos de pos_solve() hay que re-derivarlo para diferencias
             * antes de que signifique algo. Trabajo futuro, no olvido. */
            out->dropped_idx = POS_NO_DROP;
            out->valid = true;
            return true;
        }
    }
    return false;   /* no convergio */
}
```

- [ ] **Step 5: Correr el test y verificar que pasa**

```bash
gcc -Wall -Wextra -Isrc -o tests/tdoa_solve/test_tdoa_solve.exe \
    tests/tdoa_solve/test_tdoa_solve.c src/tdoa_solve.c src/pos_residual.c -lm && \
    ./tests/tdoa_solve/test_tdoa_solve.exe
```
Esperado: `tdoa_solve: ALL TESTS PASSED`. **Anotar el número que imprime
`test_error_under_gate_level_noise`** — es la traducción del gate de Fase 2 a
error de posición, y va en el spec.

- [ ] **Step 6: Control negativo**

Romper deliberadamente el signo de la referencia (usar `r_0 − r_i` en vez de
`r_i − r_0`) y confirmar que `test_recovers_a_known_position` **falla**. Revertir.
Un solver que "pasa" con el signo invertido significa que los tests no lo están
midiendo.

- [ ] **Step 7: Registrar, build, commit**

Añadir `src/tdoa_solve.c` a `CMakeLists.txt` (entre `src/tag_id.c` y
`src/uwb_config.c`), `.gitignore` en el directorio de test, entradas en
`CLAUDE.md`. Correr `west build -d build_prod`; esperado 0 errores/warnings.

```bash
git add src/tdoa_solve.* tests/tdoa_solve CMakeLists.txt CLAUDE.md
git commit -m "feat(tdoa): hyperbolic 2D solver over range differences

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

## Task 4: El colector de observaciones

Agrupa las observaciones que distintas anclas hacen **del mismo blink**. C puro
y host-testeable; es donde viven las decisiones de "cuánto esperar" y "con
cuántas anclas me conformo".

**Files:**
- Create: `src/tdoa_collect.h`, `src/tdoa_collect.c`
- Test: `tests/tdoa_collect/test_tdoa_collect.c`
- Modify: `CMakeLists.txt`, `CLAUDE.md`

**Interfaces:**
- Consumes: `struct tdoa_meas` de `tdoa_solve.h` (Tarea 3).
- Produces: `TDOA_COLLECT_SLOTS` (`16`), `TDOA_COLLECT_WINDOW_MS` (`150`),
  `struct tdoa_obs { uint16_t tag_addr; uint8_t blink_seq; uint8_t anchor_id; struct tdoa_meas meas; }`,
  y las dos estructuras de estado:

```c
struct tdoa_group {
    uint16_t tag_addr;
    uint8_t  blink_seq;
    uint8_t  n;                              /* observaciones reunidas */
    uint32_t anchor_bits;                    /* anchor_id ya vistos */
    uint32_t first_ms;                       /* llegada de la primera */
    bool     used;
    struct tdoa_meas meas[POS_MAX_ANCHORS];
};

struct tdoa_collect {
    struct tdoa_group slot[TDOA_COLLECT_SLOTS];
};
```

  `void tdoa_collect_init(struct tdoa_collect *c)`,
  `bool tdoa_collect_add(struct tdoa_collect *c, const struct tdoa_obs *o, uint32_t now_ms)`,
  `bool tdoa_collect_take_ready(struct tdoa_collect *c, uint32_t now_ms, struct tdoa_meas *out, size_t *n_out, uint16_t *tag_addr_out)`.

Decisiones de diseño que el implementador **no** debe re-litigar:

- **La clave es `(tag_addr, blink_seq)`.** `blink_seq` da la vuelta en 256, que a
  5 Hz son 51 s — muchísimo más que la ventana, así que no hace falta más.
- **`TDOA_COLLECT_WINDOW_MS = 150`.** Menor que un superframe (200 ms) a
  propósito: un blink se resuelve o se descarta antes del siguiente. Un fix
  atrasado no vale nada en un RTLS.
- **Descartar el más viejo cuando no hay ranura libre**, igual que
  `net_uplink_submit()` y por la misma razón.
- **Mínimo `TDOA_MIN_ANCHORS` observaciones** para entregar un grupo. Menos no
  es resoluble en 2D.
- **Reloj en milisegundos con diferencias con signo**, para que dé la vuelta
  libremente.

- [ ] **Step 1: Escribir el test que falla**

Crear `tests/tdoa_collect/test_tdoa_collect.c` cubriendo, cada uno como función
propia con `CHECK()`:

1. `test_groups_by_tag_and_seq` — tres observaciones del mismo `(tag, seq)` se
   entregan juntas; una cuarta con `seq` distinto **no** se mezcla.
2. `test_two_tags_do_not_mix` — mismo `seq`, `tag_addr` distinto, dos grupos.
3. `test_below_minimum_is_not_ready` — dos observaciones no se entregan.
4. `test_window_expiry_drops_the_group` — un grupo incompleto a
   `now_ms + TDOA_COLLECT_WINDOW_MS + 1` desaparece y libera su ranura.
5. `test_slot_exhaustion_drops_oldest` — `TDOA_COLLECT_SLOTS + 1` grupos
   simultáneos: el más viejo se va, el más nuevo entra.
6. `test_duplicate_anchor_is_ignored` — la misma `anchor_id` dos veces para el
   mismo blink cuenta una sola vez. **Esto importa de verdad**: un duplicado
   metería una ecuación con diferencia cero contra sí misma y volvería singular
   la matriz normal.
7. `test_ms_clock_wrap` — el mismo escenario que 4 con `now_ms` cerca de
   `UINT32_MAX`, cruzando la vuelta.

- [ ] **Step 2: Correr y verificar que no compila**

```bash
gcc -Wall -Wextra -Isrc -o tests/tdoa_collect/test_tdoa_collect.exe \
    tests/tdoa_collect/test_tdoa_collect.c src/tdoa_collect.c -lm
```
Esperado: FALLA por `tdoa_collect.h` inexistente.

- [ ] **Step 3: Escribir header e implementación**

Estructura: un arreglo de `TDOA_COLLECT_SLOTS` grupos, cada uno con
`tag_addr`, `blink_seq`, `first_ms`, un arreglo de hasta `POS_MAX_ANCHORS`
`struct tdoa_meas`, un bitmap de `anchor_id` ya vistos, y un contador. Búsqueda
lineal — 16 ranuras, corre una vez por observación.

- [ ] **Step 4: Correr los tests y verificar que pasan**

```bash
gcc -Wall -Wextra -Isrc -o tests/tdoa_collect/test_tdoa_collect.exe \
    tests/tdoa_collect/test_tdoa_collect.c src/tdoa_collect.c -lm && \
    ./tests/tdoa_collect/test_tdoa_collect.exe
```

- [ ] **Step 5: Prueba de extremo a extremo, colector + solver**

Añadir al final de la suite un test que alimente observaciones sintéticas de 4
anclas (las mismas del test de la Tarea 3), tome el grupo y lo pase a
`tdoa_solve()`, comprobando que recupera la posición. Es lo que prueba que las
dos piezas **encajan**, no solo que cada una pasa sus propios tests.

```bash
gcc -Wall -Wextra -Isrc -o tests/tdoa_collect/test_tdoa_collect.exe \
    tests/tdoa_collect/test_tdoa_collect.c src/tdoa_collect.c \
    src/tdoa_solve.c src/pos_residual.c -lm && \
    ./tests/tdoa_collect/test_tdoa_collect.exe
```

- [ ] **Step 6: Registrar, build, commit**

```bash
git add src/tdoa_collect.* tests/tdoa_collect CMakeLists.txt CLAUDE.md
git commit -m "feat(tdoa): group blink observations across anchors

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Nota de cobertura: el EKF queda fuera, a propósito

La tarea A9 del spec dice portar `pos_solver` / `pos_residual` / **`pos_ekf`**.
Este plan porta los dos primeros y **difiere el tercero**, lo cual es una
desviación del spec y va dicha en vez de escondida.

Razón: `pos_ekf.c` es un EKF *fuertemente acoplado sobre rangos crudos*, así que
portarlo a TDoA no es una copia — su modelo de medición cambia igual que el del
solver, y es más trabajo que `tdoa_solve.c` porque además lleva estado
(`[x, y, vx, vy]`), ZUPT y gating de innovación por medición. Meterlo en la
Fase 3 mezcla dos cosas separables: *obtener* un fix TDoA correcto, y *filtrarlo*.

El orden correcto es fix primero, filtro después: sin fixes crudos que mirar no
hay forma de sintonizar el filtro ni de saber si mejora algo. Cuando el gateway
esté resolviendo en vivo, el EKF es una tarea propia con sus propios host tests,
y su valor se mide contra los fixes crudos que la Fase 3 empieza a producir.

`pos_ekf.c` **no se borra del tag** mientras tanto (ver Tarea 8).

---

## Hasta aquí llega el adelanto

Las tareas 1–4 son C puro, host-testeable, y **no dependen del gate**. Al
terminarlas existe un camino TDoA completo y probado desde observaciones
sintéticas hasta una posición resuelta. Lo que falta es conectarlo a radio y a
red — y eso sí depende de que el gate pase.

**Detenerse aquí y correr el gate antes de seguir.**

---

## Task 5: MQTT subscribe y uplink en modo SLAVE *(requiere gate)*

**Files:**
- Modify: `src/net_uplink.c`, `src/net_uplink.h`, `src/main.c`
- Create: `src/blink_rx.{c,h}`
- Modify: `src/uwb_slave.c`, `src/pos_json.{c,h}`

**Hallazgo que hay que enfrentar primero:** `net_uplink.c` **no tiene ninguna
capacidad de subscribe** — es solo-publish (verificado: cero coincidencias de
`mqtt_subscribe` o `MQTT_EVT_PUBLISH`). Que el gateway resuelva exige que
*reciba*, así que esto es una capacidad nueva, no una extensión. Presupuestarlo
como tal.

Y arrancar `net_uplink` en modo SLAVE contradice su propio comentario de
cabecera actual ("GATEWAY mode only — a slave has nothing to publish and
starting WiFi there would cost ~50 KB of heap for no benefit"). Ese comentario
deja de ser cierto en la Fase 3 y **hay que reescribirlo**, no ignorarlo. Medir
el heap y la RAM en el ancla: hoy DRAM está al 65 %.

Sub-pasos, cada uno con su commit:

- [ ] Tópico y payload de observación en `pos_json.{c,h}` (C puro, host-testeable
      como el resto de `pos_json`): `uwb/anchor/blink/<zone>`, con
      `{"a":<anchor_id>,"t":<tag_addr>,"s":<seq>,"ts":<t_dtu>,"q":<quality>}`.
      `ts` es un entero de 40 bits — **verificar que el consumidor no lo trunca**,
      la lección del `Tid` a `int32`.
- [ ] `blink_rx.{c,h}`: en `cbRxOk`, si `blink_frame_is_blink()`, leer
      `dwt_readrxtimestamp()`, convertir con `sync_model_to_master()`, y **si el
      modelo no es válido, descartar** — una observación sin tiempo común es
      ruido, no dato.
- [ ] Despacho de `0xF0` en `uwb_slave.c`.
- [ ] `net_uplink` en modo SLAVE + subscribe en el gateway.

## Task 6: Resolver en el gateway *(requiere gate)*

**Files:** Create `src/tdoa_gw.{c,h}`; modify `src/uwb_gateway.c`.

- [ ] Alimentar `tdoa_collect_add()` desde el callback de MQTT.
- [ ] Llamar `tdoa_collect_take_ready()` desde el loop del gateway **una vez por
      iteración, con cota** — es `K_PRIO_COOP(0)` y el beacon manda.
- [ ] Resolver con `tdoa_solve()`, rellenar `struct pos_fix` (incluido
      `tag_id_from_eui()` para el `Tid`) y llamar `pos_sink_publish()`.
      **Río abajo no cambia nada**: mismo `pos_fix`, mismo `pos_json_fix()`,
      mismo contrato con la plataforma.

## Task 7: El tag emite blink *(requiere gate)*

**Files:** en `tag_testting`: copiar `blink_frame.{c,h}`; modificar
`src/uwb_net_runner.c`, `src/uwb_net.c`.

- [ ] Copiar `blink_frame.{c,h}` desde ANCLA; `diff` vacío.
- [ ] Sustituir `UWB_ACT_RUN_SWEEP` por emitir un BLINK en el slot concedido.
- [ ] **No borrar** `pos_solver` / `pos_ekf` del tag: son la fuente del porte y
      de sus host tests.
- [ ] Medir el consumo. Es el premio de la Fase 3 del lado del tag: TX-only en
      vez de cuatro intercambios TWR con RX abierto.

## Task 8: Limpieza *(requiere gate y Tarea 7 en hardware)*

- [ ] Borrar `pos_dbg.{c,h}` y `tests/pos_dbg/` según el checklist de remoción de
      `spec/2026-08-22-position-filtering-design.md`.
- [ ] Pasar la propiedad de `pos_solver` / `pos_residual` a ANCLA en ambos
      `CLAUDE.md`.
- [ ] Actualizar el spec: Fase 3 hecha, con los números medidos.

---

## Contingencias

**Si el gate sale marginal (0.5–1.0 ns):** las tareas 1–4 valen igual. Correr
`test_error_under_gate_level_noise` con el ruido medido de verdad en vez de
±32 DTU; ése es el error de posición real. Si queda dentro de 10–30 cm, seguir.
Antes de rediseñar, aplicar la escalera de remedios de §5 de
`docs/anchor-sync-measurement.md`, que está ordenada por costo — subir
`SYNC_PHASE_EMA_SHIFT` es gratis en airtime.

**Si el gate falla (> 1 ns):** las tareas 1–4 **no se tiran**. El solver TDoA y
el colector siguen siendo correctos y quedan listos para una revisión de
hardware con sincronía cableada. Se detienen las tareas 5–8 y se pasa a la
**Fase 4** (`A10`/`A11`: downlink `0xEC`/`0xED` y zona/PANID en NVS), que **no
depende del gate** y da tags configurables desde la plataforma y despliegue
multi-celda con un solo binario. El producto queda en TWR con el scheduler de
la Fase 1 — 128 tags presentes, 6 movedores a 5 Hz.

**Si las tareas 1–4 revelan que el modelo de medición está mal:** ese es
precisamente el valor de escribirlas antes. Corregir aquí cuesta un ciclo de
tests; corregirlo después de cablear radio y MQTT cuesta una sesión de banco.
